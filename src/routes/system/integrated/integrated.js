import React from 'react';
import { Screen } from '../../../components/base';

export const IntegratedTheroy = ({}) => {
    return (
        <Screen>
            Integrated Theory
        </Screen>
    )
};

export default IntegratedTheroy;