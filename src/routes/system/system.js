import React from 'react';
import { Screen } from '../../components/base';

export const System = ({}) => {
    return (
        <Screen>
            System
        </Screen>
    )
};

export default System;