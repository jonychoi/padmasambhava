import React from 'react';
import {Screen} from '../../components/base';

export const Contact = ({}) => {
    return (
        <Screen bgdark="red">
        </Screen>
    )
};

export default Contact;