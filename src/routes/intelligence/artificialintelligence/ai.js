import React from 'react';
import { Screen } from '../../../components/base';

export const AI = ({}) => {
    return (
        <Screen>
            Artificial Intelligence
        </Screen>
    )
};

export default AI