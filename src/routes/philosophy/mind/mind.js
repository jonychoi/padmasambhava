import React from 'react';
import { Screen } from '../../../components/base';

export const Mind = ({}) => {
    return (
        <Screen>
            衆生心
        </Screen>
    )
};

export default Mind