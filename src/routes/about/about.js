import React from 'react';
import {Screen} from '../../components/base';

export const About = ({}) => {
    return (
        <Screen>
            About
        </Screen>
    )
};

export default About;